/**
 * This module provides a HttpClient that can be plugged into stripe-node
 * that will allow the user to use stripe-node in extensions if the Dashboard
 * provides a `stripeApiFetch` function that will relay API calls through the
 * Dashboard and piggy back on the user's Dashboard session.
 */
import type { SerializableResponse, StripeApiFetch } from './api_fetch';
import type Stripe from 'stripe';
declare class StripeTailorHttpResponse implements Stripe.HttpClientResponse {
    _resp: SerializableResponse;
    constructor(resp: SerializableResponse);
    getHeaders(): {
        [key: string]: string;
    };
    getStatusCode(): number;
    getRawResponse(): SerializableResponse;
    toStream(): void;
    toJSON(): Promise<Object>;
}
export default class StripeTailorHttpClient implements Stripe.HttpClient {
    _fetch: StripeApiFetch;
    constructor(fetch: StripeApiFetch);
    getClientName(): string;
    makeRequest(host: string, port: string | number, path: string, method: 'GET' | 'POST' | 'PUT' | 'DELETE', headers?: object, requestData?: string | null, protocol?: 'http' | 'https', timeout?: number): Promise<StripeTailorHttpResponse>;
}
export declare const STRIPE_API_KEY = "DO_NOT_PASS_A_REAL_API_KEY";
export declare const createHttpClient: () => StripeTailorHttpClient;
export {};
