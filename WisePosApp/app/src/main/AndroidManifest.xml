<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios para Stripe Terminal -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Permiso para Apps on Device en WisePOS E -->
    <uses-permission android:name="com.stripe.terminal.HANDOFF" />

    <application
        android:name=".JeturingPayApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JeturingPayTerminal"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.JeturingPayTerminal.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Intent filter para Apps on Device -->
            <intent-filter>
                <action android:name="com.stripe.terminal.HANDOFF" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Actividad de cobro -->
        <activity
            android:name=".PaymentActivity"
            android:exported="false"
            android:theme="@style/Theme.JeturingPayTerminal" />

        <!-- Actividad de historial -->
        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.JeturingPayTerminal" />

        <!-- Servicio de Apps on Device -->
        <service
            android:name=".service.TerminalHandoffService"
            android:exported="true"
            android:permission="com.stripe.terminal.permission.HANDOFF">
            <intent-filter>
                <action android:name="com.stripe.terminal.HANDOFF_SERVICE" />
            </intent-filter>
        </service>

    </application>

</manifest>
